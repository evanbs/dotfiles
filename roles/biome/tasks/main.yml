---
- name: Install Biome.js globally
  become_user: "{{ dev_username }}"
  shell: |
    set -euo pipefail
    . "/home/{{ dev_username }}/.nvm/nvm.sh"
    nvm use default
    npm install -g @biomejs/biome
  args:
    executable: /bin/bash
  tags:
    - install
    - biome
