---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu-fnm-test
    image: ubuntu:22.04
    pre_build_image: false
provisioner:
  name: ansible
  inventory:
    host_vars:
      ubuntu-fnm-test:
        dev_username: testuser
        brew_prefix: /home/linuxbrew/.linuxbrew
verifier:
  name: ansible
